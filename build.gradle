buildscript {
    ext.vSpringBoot = '0.5.0.M4'
    ext.vSpringCore = '4.0.0.M3'
    ext.vHttpClient = '4.3-beta2'

    repositories {
        maven { url 'http://repo.spring.io/libs-milestone' }
    }
    dependencies {
        classpath "org.springframework.boot:spring-boot-gradle-plugin:${vSpringBoot}"
    }
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'

    targetCompatibility = 1.7
    sourceCompatibility = 1.7

    repositories {
        jcenter()
        maven { url 'http://repo.spring.io/libs-milestone' }
    }

    dependencies {
        compile 'org.slf4j:slf4j-api:1.7.5'
        testCompile 'junit:junit:4.11'
        testCompile 'org.hamcrest:hamcrest-library:1.3'
        testCompile 'org.mockito:mockito-core:1.9.5'
    }
}

/*
gradle.projectsEvaluated {
    gradle.taskGraph.whenReady { taskGraph ->
        def unitTestTasks = taskGraph.allTasks.findAll { it.name == 'test' }
        println taskGraph.allTasks
    }
}
*/

gradle.projectsEvaluated {
    project(':sagan-site').tasks['it'].mustRunAfter project(':sagan-indexer').test
    project(':sagan-indexer').tasks['it'].mustRunAfter project(':sagan-site').test
    project(':sagan-site').deployStaging.mustRunAfter project(':sagan-indexer').check
    project(':sagan-indexer').deployStaging.mustRunAfter project(':sagan-site').check
}
